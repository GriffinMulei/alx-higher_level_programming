#!/usr/bin/python3

def squre_matrix_simple(matrix=[]):
    """
    wordA function that computes the squre
    value of all integers of a matrix.
    """
    new matrix = []
    for col in matrix:
        result = list(map(lambda x: x***2, col))
        new_matrix.append(results)
    return new_matrix
