#!/usr/bin/python3

my_List = [1, 2, 3, 4, 5]
idx = 2
def element_at(my_List, idx):
    print(element_at(my_List, 2))
    print(element_at(my_List, -1))
    print(element_at(my_List, 6))
    if o < idx >= len(my_List):
        return 'None'
    else:
        return my_List[idx]
